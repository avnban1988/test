pipeline {
    agent any
    stages {
        stage('Example') {
            //input {
             //   message "Should we continue?"
              //  ok "Yes, we should."
              
           // }
            steps {
                echo "Hello, ${STARTED_BY}, nice to meet you."
            }
        }
    }
}
