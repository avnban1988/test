pipeline {
    agent any
    stages {
        stage('Example') {
            //input {
             //   message "Should we continue?"
              //  ok "Yes, we should."
              
           // }
            steps {
               sh 'echo "${BUILD_USER}"'
            }
        }
    }
}
